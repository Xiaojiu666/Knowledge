 ### 字节
 字节（Byte）是计算机信息技术用于计量存储容量的一种计量单位，也表示一些计算机编程语言中的数据类型和语言字符，一个字节存储8位无符号数，储存的数值范围为0-255。

 ### 字节数组
byte[]里面可以存放2、10、16进制数，8好像不行 但用的时候，都自动转为十进制，
```Java
  //自动匹配ASCII表里面的值
  byte[] byte = {'H','L','K','J'};
  byte[0] = 72
  // 不能超过 8位表示的 最大值和最小值
  byte[] head_name1 = {1,-128,127};
```
