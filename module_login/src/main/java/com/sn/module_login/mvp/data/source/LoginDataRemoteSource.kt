package com.sn.module_login.mvp.data.source

class LoginDataRemoteSource : LoginDataSource {
    override fun login(userName: String, passWord: String) {

    }

    override fun register(userName: String, passWord: String, verCode: Int) {
    }

    override fun modifyPassWord(userName: String, passWord: String, verCode: Int) {
    }
}